name: Run Scraper

on:
  workflow_dispatch:
    inputs:
      run_mode:
        description: "Choose run mode"
        required: true
        default: "full"
        type: choice
        options:
          - full
          - test
          - baseline

      url_file:
        description: "CSV file to run"
        required: true
        default: "documents.csv"
        type: choice
        options:
          - documents.csv
          - test_urls.csv
          - baseline_urls.csv

      enable_browser_fallback:
        description: "Use browser fallback for selected zero-doc URLs?"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

      fallback_domains:
        description: "Comma-separated domains for browser fallback"
        required: false
        default: "chandra-asri.com"
        type: string

jobs:
  run-script:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    env:
      RUN_MODE: ${{ github.event.inputs.run_mode }}
      TARGET_URL_FILE: ${{ github.event.inputs.url_file }}
      ENABLE_BROWSER_FALLBACK: ${{ github.event.inputs.enable_browser_fallback }}
      FALLBACK_DOMAINS: ${{ github.event.inputs.fallback_domains }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 playwright
          playwright install chromium

      - name: Run script
        run: python scrape.py

      - name: Commit and push output files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add output*.csv raw_links*.csv capture_issues*.csv diff.csv run_summary.csv 2>/dev/null || true

          git commit -m "Update scraped data" || echo "No changes to commit"

          git push
